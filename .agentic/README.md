# Agent engineering plane

This directory holds deterministic engineering policy and reusable workflow guidance. Generated indexes and runtime evidence are intentionally ignored by Git and must never be treated as manually maintained semantic truth.

