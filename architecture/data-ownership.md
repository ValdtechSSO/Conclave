# Data ownership

Conclave owns run artifacts beneath its configured home and snapshot refs beneath `refs/conclave/runs/`. Target repositories own all source content. Optional `--output` publication is the only intended write into a target repository.

